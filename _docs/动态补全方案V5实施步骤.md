# 动态补全方案 V5 实施步骤

## 概述

本文档详细描述动态补全方案 V5 的实施步骤，按照依赖关系和逻辑顺序分为多个阶段。

## 实施阶段

### 阶段 1: 基础类型定义

**目标**: 在 types 包中添加必要的接口和常量定义

**文件**: `internal/types/command.go`

**步骤**:
1. 在 Command 接口中添加 IsHidden() 方法
2. 添加 Completer 函数类型定义（如果需要）

**代码示例**:
```go
// Command 接口添加方法
type Command interface {
    // ... 现有方法 ...
    
    // IsHidden 返回命令是否隐藏
    IsHidden() bool
}
```

---

### 阶段 2: 配置结构体更新

**目标**: 添加 DynamicCompletion 字段到配置结构体

#### 2.1 CmdConfig 结构体

**文件**: `internal/types/config.go`

**步骤**:
1. 在 CmdConfig 结构体中添加 DynamicCompletion 字段
2. 更新 Clone() 方法，复制新字段

**代码示例**:
```go
type CmdConfig struct {
    // ... 现有字段 ...
    
    // DynamicCompletion 是否启用动态补全
    DynamicCompletion bool
}

// Clone 方法更新
func (c *CmdConfig) Clone() *CmdConfig {
    // ... 现有逻辑 ...
    return &CmdConfig{
        // ... 复制现有字段 ...
        DynamicCompletion: c.DynamicCompletion,
    }
}
```

#### 2.2 CmdOpts 结构体

**文件**: `internal/cmd/cmdopts.go`

**步骤**:
1. 在 CmdOpts 结构体中添加 DynamicCompletion 字段

**代码示例**:
```go
type CmdOpts struct {
    // ... 现有字段 ...
    
    // DynamicCompletion 是否启用动态补全
    DynamicCompletion bool
}
```

---

### 阶段 3: completion 包核心实现

**目标**: 实现动态补全的核心逻辑（所有逻辑都在 completion 包中）

#### 3.1 模糊匹配算法

**文件**: `internal/completion/fuzzy.go` (新建)

**步骤**:
1. 创建 fuzzy.go 文件
2. 实现 FuzzyScore 函数
3. 实现 IntelligentMatches 函数

**代码示例**:
```go
package completion

// FuzzyScore 计算模糊匹配分数 (0-100)
func FuzzyScore(pattern, candidate string) int {
    // 实现模糊匹配算法
}

// IntelligentMatches 智能匹配（渐进式策略）
func IntelligentMatches(pattern string, options []string) []string {
    // 实现渐进式匹配
}
```

#### 3.2 补全上下文

**文件**: `internal/completion/context.go` (新建)

**步骤**:
1. 创建 context.go 文件
2. 定义 Context 结构体
3. 实现 NewContext 函数
4. 实现 resolveCurrentCmd 函数

**代码示例**:
```go
package completion

import (
    "strings"
    "gitee.com/MM-Q/qflag/internal/types"
)

// Context 补全上下文
type Context struct {
    RootCmd        types.Command
    CurrentCmd     types.Command
    WordToComplete string
    PreviousWord   string
    Args           []string
}

// NewContext 创建补全上下文
func NewContext(root types.Command, wordToComplete, previousWord string, args []string) *Context {
    // 实现上下文创建
}

// resolveCurrentCmd 解析当前命令
func resolveCurrentCmd(root types.Command, args []string) types.Command {
    // 实现命令解析
}
```

#### 3.3 动态补全执行逻辑

**文件**: `internal/completion/dynamic.go` (新建)

**步骤**:
1. 创建 dynamic.go 文件
2. 实现 Execute 函数
3. 实现 isTypingFlagValue 函数
4. 实现 completeFlagValue 函数
5. 实现 completeCommandOrFlag 函数
6. 实现 completeFromOptions 函数
7. 实现 completePath 函数

**代码示例**:
```go
package completion

import (
    "os"
    "strings"
    "gitee.com/MM-Q/qflag/internal/types"
)

// Execute 执行补全
func Execute(ctx *Context) types.CompletionResult {
    // 判断输入类型并执行相应补全
}

// 其他辅助函数...
```

#### 3.4 执行入口

**文件**: `internal/completion/execute.go` (新建)

**步骤**:
1. 创建 execute.go 文件
2. 实现 RunComplete 函数（供 cmd 包调用）

**代码示例**:
```go
package completion

import (
    "fmt"
    "gitee.com/MM-Q/qflag/internal/types"
)

// RunComplete 执行动态补全（供 cmd 包调用）
func RunComplete(cmd types.Command, root types.Command) error {
    // 解析参数
    // 构建上下文
    // 执行补全
    // 输出结果
}
```

---

### 阶段 4: cmd 包集成

**目标**: 在 cmd 包中添加动态补全支持

#### 4.1 Cmd 结构体更新

**文件**: `internal/cmd/cmd.go`

**步骤**:
1. 在 Cmd 结构体中添加 completer 和 hidden 字段
2. 实现 SetCompleter 方法
3. 实现 SetHidden 方法
4. 实现 IsHidden 方法

**代码示例**:
```go
type Cmd struct {
    // ... 现有字段 ...
    
    completer func(ctx *completion.Context) []string
    hidden    bool
}

func (c *Cmd) SetCompleter(completer func(ctx *completion.Context) []string) {
    c.completer = completer
}

func (c *Cmd) SetHidden(hidden bool) {
    c.hidden = hidden
}

func (c *Cmd) IsHidden() bool {
    return c.hidden
}
```

#### 4.2 ApplyOpts 方法更新

**文件**: `internal/cmd/cmd.go`

**步骤**:
1. 在 ApplyOpts 方法中添加 DynamicCompletion 处理

**代码示例**:
```go
func (c *Cmd) ApplyOpts(opts *CmdOpts) {
    // ... 现有逻辑 ...
    
    if opts.DynamicCompletion {
        c.SetDynamicCompletion(true)
    }
}
```

#### 4.3 __complete 子命令注册

**文件**: `internal/cmd/complete.go` (新建)

**步骤**:
1. 创建 complete.go 文件
2. 定义 CompleteCmdName 常量
3. 实现 registerCompleteCmd 方法

**代码示例**:
```go
package cmd

import "gitee.com/MM-Q/qflag/internal/completion"

const CompleteCmdName = "__complete"

func (c *Cmd) registerCompleteCmd() {
    // 避免重复注册
    if _, exists := c.cmdRegistry.Get(CompleteCmdName); exists {
        return
    }
    
    cmd := NewCmd(CompleteCmdName, "", c.config.ErrorHandling)
    cmd.SetDesc("内部命令：执行动态补全")
    cmd.SetHidden(true)
    
    cmd.SetRun(func(cmd Command) error {
        return completion.RunComplete(cmd, c)
    })
    
    c.AddSubCmd(cmd)
}
```

#### 4.4 SetDynamicCompletion 方法

**文件**: `internal/cmd/cmd.go`

**步骤**:
1. 实现 SetDynamicCompletion 方法

**代码示例**:
```go
func (c *Cmd) SetDynamicCompletion(enabled bool) {
    c.config.DynamicCompletion = enabled
    if enabled {
        c.registerCompleteCmd()
    }
}
```

---

### 阶段 5: 脚本生成逻辑

**目标**: 添加动态/静态脚本生成选择逻辑

**文件**: `internal/completion/completion.go`

**步骤**:
1. 修改 GenerateScript 函数，添加动态/静态选择
2. 实现 generateDynamicScript 函数
3. 实现 generateStaticScript 函数（包装现有逻辑）

**代码示例**:
```go
// GenerateScript 生成补全脚本
func GenerateScript(cmd types.Command, shell string, config *types.CmdConfig) (string, error) {
    if config.DynamicCompletion {
        return generateDynamicScript(cmd, shell)
    }
    return generateStaticScript(cmd, shell)
}

func generateDynamicScript(cmd types.Command, shell string) (string, error) {
    switch shell {
    case "bash":
        return generateBashDynamicScript(cmd)
    case "pwsh", "powershell":
        return generatePwshDynamicScript(cmd)
    default:
        return "", fmt.Errorf("不支持的 shell 类型: %s", shell)
    }
}

func generateStaticScript(cmd types.Command, shell string) (string, error) {
    // 调用现有静态脚本生成逻辑
}
```

---

### 阶段 6: 动态补全脚本模板

**目标**: 创建极简的动态补全脚本模板

#### 6.1 Bash 动态模板

**文件**: `internal/completion/templates/bash_dynamic.tmpl` (新建)

**步骤**:
1. 创建 bash_dynamic.tmpl 文件
2. 编写极简 Bash 补全脚本模板

**模板内容**:
```bash
#!/usr/bin/env bash

_{{.ProgramName}}_complete() {
    local cur prev words cword
    _init_completion || return
    
    local completions
    completions=$({{.ProgramName}} __complete "$cur" "$prev" ${words[@]} 2>/dev/null)
    
    local directive
    directive=$(echo "$completions" | tail -n 1)
    
    local candidates
    candidates=$(echo "$completions" | head -n -1)
    
    COMPREPLY=($(compgen -W "$candidates" -- "$cur"))
}

complete -F _{{.ProgramName}}_complete {{.ProgramName}}
```

#### 6.2 PowerShell 动态模板

**文件**: `internal/completion/templates/pwsh_dynamic.tmpl` (新建)

**步骤**:
1. 创建 pwsh_dynamic.tmpl 文件
2. 编写极简 PowerShell 补全脚本模板

**模板内容**:
```powershell
Register-ArgumentCompleter -Native -CommandName {{.ProgramName}} -ScriptBlock {
    param($wordToComplete, $commandAst, $cursorPosition)
    
    $words = $commandAst.CommandElements | ForEach-Object { $_.ToString() }
    $prev = if ($words.Count -gt 1) { $words[$words.Count - 2] } else { "" }
    
    $completions = & {{.ProgramName}} __complete $wordToComplete $prev @words 2>$null
    
    if (-not $completions) { return }
    
    $lines = $completions -split "`n"
    $candidates = $lines[0..($lines.Count - 2)]
    
    foreach ($candidate in $candidates) {
        $parts = $candidate -split "`t", 2
        [System.Management.Automation.CompletionResult]::new(
            $parts[0], $parts[0], 'ParameterValue', 
            if ($parts[1]) { $parts[1] } else { $parts[0] }
        )
    }
}
```

---

### 阶段 7: 帮助生成调整

**目标**: 使用 IsHidden() 方法跳过隐藏命令

**文件**: `internal/help/help.go`

**步骤**:
1. 在生成子命令列表时，检查 IsHidden()
2. 跳过隐藏命令

**代码示例**:
```go
// 生成子命令列表
for _, subCmd := range cmd.SubCmds() {
    if subCmd.IsHidden() {
        continue
    }
    // 生成子命令帮助信息
}
```

---

### 阶段 8: 测试验证

**目标**: 验证动态补全功能正常工作

#### 8.1 单元测试

**文件**: `internal/completion/fuzzy_test.go` (新建)

**测试内容**:
- FuzzyScore 函数测试
- IntelligentMatches 函数测试

#### 8.2 集成测试

**步骤**:
1. 创建测试程序
2. 启用动态补全
3. 生成补全脚本
4. 测试补全功能

**测试代码示例**:
```go
package main

import (
    "os"
    "gitee.com/MM-Q/qflag"
)

func main() {
    root := qflag.NewCmd("testapp", "测试应用", qflag.ExitOnError)
    root.SetCompletion(true)
    root.SetDynamicCompletion(true)
    
    // 添加子命令和标志...
    
    root.ParseAndRoute(os.Args[1:])
}
```

**测试命令**:
```bash
# 生成动态补全脚本
testapp --completion=bash

# 测试补全
testapp __complete "" "" testapp
```

---

## 实施检查清单

### 阶段 1 检查项
- [ ] Command 接口添加 IsHidden() 方法

### 阶段 2 检查项
- [ ] CmdConfig 添加 DynamicCompletion 字段
- [ ] CmdConfig.Clone() 复制新字段
- [ ] CmdOpts 添加 DynamicCompletion 字段

### 阶段 3 检查项
- [ ] fuzzy.go 实现模糊匹配算法
- [ ] context.go 实现补全上下文
- [ ] dynamic.go 实现补全逻辑
- [ ] execute.go 实现执行入口

### 阶段 4 检查项
- [ ] Cmd 结构体添加 completer 和 hidden 字段
- [ ] 实现 SetCompleter 方法
- [ ] 实现 SetHidden 方法
- [ ] 实现 IsHidden 方法
- [ ] ApplyOpts 处理 DynamicCompletion
- [ ] complete.go 实现子命令注册
- [ ] 实现 SetDynamicCompletion 方法

### 阶段 5 检查项
- [ ] GenerateScript 添加动态/静态选择
- [ ] 实现 generateDynamicScript
- [ ] 实现 generateStaticScript

### 阶段 6 检查项
- [ ] 创建 bash_dynamic.tmpl
- [ ] 创建 pwsh_dynamic.tmpl

### 阶段 7 检查项
- [ ] 帮助生成使用 IsHidden() 跳过隐藏命令

### 阶段 8 检查项
- [ ] 模糊匹配单元测试
- [ ] 集成测试通过

---

## 注意事项

1. **依赖关系**: 严格按照阶段顺序实施，后一阶段依赖前一阶段
2. **接口一致性**: 确保 Command 接口的所有实现都更新
3. **线程安全**: 注意并发情况下的锁处理
4. **向后兼容**: 确保静态补全功能不受影响
5. **错误处理**: 完善错误处理和边界情况

---

## 参考文档

- [动态补全方案 V5](动态补全方案V5.md)
- [静态补全脚本模板](../internal/completion/templates/bash.tmpl)
- [静态补全脚本模板](../internal/completion/templates/pwsh.tmpl)
