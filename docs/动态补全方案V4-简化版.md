# qflag 动态补全方案 V4 简化版

## 设计思路

简化用户接口，只需要一个 `--completion` 标志即可生成静态或动态补全脚本。

## 核心变化

### 1. 用户接口简化

**之前**:
```bash
# 静态补全
myapp --completion=bash

# 动态补全
myapp --completion=bash --comp-mode=dynamic
```

**现在**:
```bash
# 统一接口
myapp --completion=bash

# 生成静态还是动态脚本，由程序内部配置决定
```

### 2. 生成逻辑

程序根据两个配置字段决定生成什么脚本：

```go
if config.Completion {
    if config.DynamicCompletion {
        // 生成动态补全脚本
        generateDynamicScript()
    } else {
        // 生成静态补全脚本
        generateStaticScript()
    }
}
```

### 3. 动态补全脚本内部逻辑

生成的动态补全脚本直接调用 `__complete` 子命令：

```bash
# Bash 动态补全脚本示例
_myapp_completion() {
    local cur prev words cword
    _init_completion || return
    
    # 直接调用 __complete 子命令
    # 参数: <当前词> <前一个词> <完整命令行>
    local completions
    completions=$(myapp __complete "$cur" "$prev" ${words[@]} 2>/dev/null)
    
    # 解析结果...
}
```

## 实现方案

### 1. 修改补全脚本生成逻辑

**位置**: `internal/completion/completion.go`

```go
// Generate 生成补全脚本
//
// 参数:
//   - cmd: 命令实例
//   - shellType: Shell 类型
//
// 返回值:
//   - string: 生成的脚本
//   - error: 错误
func Generate(cmd types.Command, shellType string) (string, error) {
    config := cmd.Config()
    
    // 检查是否启用补全
    if !config.Completion {
        return "", fmt.Errorf("completion not enabled")
    }
    
    // 根据配置决定生成静态还是动态脚本
    if config.DynamicCompletion {
        return generateDynamic(cmd, shellType)
    }
    
    return generateStatic(cmd, shellType)
}

// generateStatic 生成静态补全脚本
func generateStatic(cmd types.Command, shellType string) (string, error) {
    // 现有静态补全逻辑
    // ...
}

// generateDynamic 生成动态补全脚本
func generateDynamic(cmd types.Command, shellType string) (string, error) {
    programName := filepath.Base(os.Args[0])
    
    switch shellType {
    case types.BashShell:
        return generateDynamicBash(programName)
    case types.PwshShell, types.PowershellShell:
        return generateDynamicPwsh(programName)
    default:
        return "", fmt.Errorf("unsupported shell type '%s'", shellType)
    }
}
```

### 2. 动态补全脚本模板

**Bash 动态模板** (`templates/bash_dynamic.tmpl`):

```bash
# {{.ProgramName}} completion script (dynamic)
# Generated by qflag

_{{.ProgramName}}_completion() {
    local cur prev words cword
    _init_completion || return

    # 调用 __complete 子命令获取补全结果
    # 参数: <当前词> <前一个词> <完整命令行>
    local completions
    completions=$({{.ProgramName}} __complete "$cur" "$prev" ${words[@]} 2>/dev/null)
    
    # 解析结果（最后一行是 :指令）
    local directive
    directive=$(echo "$completions" | tail -n 1)
    
    # 提取候选值（去掉最后一行指令）
    local candidates
    candidates=$(echo "$completions" | head -n -1)
    
    # 设置补全结果
    COMPREPLY=($(compgen -W "$candidates" -- "$cur"))
    
    return 0
}

# 注册补全函数
complete -F _{{.ProgramName}}_completion {{.ProgramName}}
```

**PowerShell 动态模板** (`templates/pwsh_dynamic.tmpl`):

```powershell
# {{.ProgramName}} completion script (dynamic)
# Generated by qflag

Register-ArgumentCompleter -Native -CommandName {{.ProgramName}} -ScriptBlock {
    param($wordToComplete, $commandAst, $cursorPosition)
    
    # 获取完整的命令行参数
    $words = $commandAst.CommandElements | ForEach-Object { $_.ToString() }
    
    # 获取前一个词
    $prev = ""
    if ($words.Count -gt 1) {
        $prev = $words[$words.Count - 2]
    }
    
    # 调用 __complete 子命令获取补全结果
    # 参数: <当前词> <前一个词> <完整命令行>
    $completions = & {{.ProgramName}} __complete $wordToComplete $prev @words 2>$null
    
    if (-not $completions) {
        return
    }
    
    # 解析结果（最后一行是 :指令）
    $lines = $completions -split "`n"
    $directive = $lines[-1]
    $candidates = $lines[0..($lines.Count - 2)]
    
    # 返回候选值
    foreach ($candidate in $candidates) {
        if ($candidate -match "^$wordToComplete") {
            # 处理带描述的格式: value`tdescription
            $parts = $candidate -split "`t", 2
            [System.Management.Automation.CompletionResult]::new(
                $parts[0],
                $parts[0],
                'ParameterValue',
                if ($parts[1]) { $parts[1] } else { $parts[0] }
            )
        }
    }
}
```

### 3. 内置 `__complete` 子命令

**位置**: `internal/cmd/cmd.go`

```go
// completeCmdName 动态补全子命令名
// 使用双下划线前缀避免与用户子命令冲突，并表示为内部命令
const completeCmdName = "__complete"

// createCompleteCmd 创建动态补全子命令
//
// 返回值:
//   - *Cmd: 动态补全子命令实例
//
// 功能说明:
//   - 创建一个隐藏的子命令，用于执行动态补全
//   - 接收当前命令行作为参数
//   - 计算并返回补全结果
func createCompleteCmd() *Cmd {
    cmd := NewCmd(completeCmdName, "", ExitOnError)
    cmd.SetDesc("内部命令：执行动态补全")
    cmd.SetHidden(true) // 隐藏子命令，不在帮助中显示
    
    // 设置执行函数
    cmd.SetRun(func(c types.Command) error {
        args := c.Args()
        if len(args) < 2 {
            return fmt.Errorf("用法: __complete <wordToComplete> <previousWord> [args...]")
        }
        
        wordToComplete := args[0]
        previousWord := args[1]
        cmdArgs := args[2:]
        
        // 获取根命令（通过父命令链找到根）
        rootCmd := findRootCmd(c)
        
        // 构建补全上下文
        ctx := completion.BuildContext(rootCmd, wordToComplete, previousWord, cmdArgs)
        
        // 执行补全
        result := completion.Execute(ctx)
        
        // 输出结果
        completion.PrintResult(result)
        
        return nil
    })
    
    return cmd
}

// findRootCmd 找到根命令
func findRootCmd(cmd types.Command) types.Command {
    for {
        if cmd.IsRootCmd() {
            return cmd
        }
        // 通过父命令引用找到根
        // 需要在 Cmd 结构体中添加 parent 字段
        if c, ok := cmd.(*Cmd); ok && c.parent != nil {
            cmd = c.parent
        } else {
            return cmd
        }
    }
}
```

### 4. 启用动态补全时注册 `__complete` 子命令

**位置**: `internal/cmd/cmd.go` 中的 `SetDynamicCompletion` 方法

```go
// SetDynamicCompletion 设置是否启用动态补全
//
// 参数:
//   - enable: 是否启用动态补全
//
// 功能说明:
//   - 启用动态补全后，会注册 __complete 子命令
//   - 动态补全允许运行时调用程序生成补全结果
//   - 与静态补全独立，需要单独启用
func (c *Cmd) SetDynamicCompletion(enable bool) {
    c.mu.Lock()
    defer c.mu.Unlock()
    c.config.DynamicCompletion = enable
    
    // 如果启用动态补全，注册 __complete 子命令
    if enable {
        c.registerCompleteCmd()
    }
}

// registerCompleteCmd 注册 __complete 子命令
//
// 功能说明:
//   - 检查是否已存在同名子命令
//   - 创建并添加 __complete 子命令
func (c *Cmd) registerCompleteCmd() {
    // 检查是否已存在
    if _, exists := c.GetSubCmd(completeCmdName); exists {
        return
    }
    
    // 创建并添加 __complete 子命令
    completeCmd := createCompleteCmd()
    c.AddSubCmd(completeCmd)
}
```

### 5. 补全执行逻辑

**位置**: `internal/completion/execute.go`

```go
package completion

import (
    "fmt"
    "os"
    "strings"
    
    "gitee.com/MM-Q/qflag/internal/types"
)

// Context 补全上下文
type Context struct {
    RootCmd        types.Command
    CurrentCmd     types.Command
    WordToComplete string
    PreviousWord   string
    Args           []string
}

// BuildContext 构建补全上下文
func BuildContext(rootCmd types.Command, wordToComplete, previousWord string, args []string) *Context {
    ctx := &Context{
        RootCmd:        rootCmd,
        WordToComplete: wordToComplete,
        PreviousWord:   previousWord,
        Args:           args,
    }
    
    // 解析当前命令
    ctx.CurrentCmd = resolveCurrentCmd(rootCmd, args)
    
    return ctx
}

// resolveCurrentCmd 解析当前命令
func resolveCurrentCmd(rootCmd types.Command, args []string) types.Command {
    current := rootCmd
    for _, arg := range args {
        if strings.HasPrefix(arg, "-") {
            break
        }
        if subCmd, ok := current.GetSubCmd(arg); ok {
            current = subCmd
        } else {
            break
        }
    }
    return current
}

// Execute 执行补全
func Execute(ctx *Context) types.CompletionResult {
    // 检查是否有自定义补全函数
    if ctx.PreviousWord != "" && strings.HasPrefix(ctx.PreviousWord, "-") {
        flagName := strings.TrimLeft(ctx.PreviousWord, "-")
        if completer, ok := ctx.CurrentCmd.GetCompleter(flagName); ok {
            return completer(buildCompleterContext(ctx))
        }
    }
    
    // 默认补全逻辑
    return defaultComplete(ctx)
}

// defaultComplete 默认补全
//
// 补全优先级:
//   1. 标志值补全（如果前一个词是标志）
//   2. 标志名补全（如果当前词以 - 开头）
//   3. 子命令补全（如果当前词匹配子命令前缀）
//   4. 路径补全（默认兜底）
func defaultComplete(ctx *Context) types.CompletionResult {
    var completions []types.Completion
    
    // 判断是否正在补全标志值
    if ctx.PreviousWord != "" && strings.HasPrefix(ctx.PreviousWord, "-") {
        completions = completeFlagValue(ctx)
    } else if strings.HasPrefix(ctx.WordToComplete, "-") {
        // 补全标志名（长标志和短标志）
        completions = completeFlagName(ctx)
    } else {
        // 先尝试补全子命令
        completions = completeSubCommand(ctx)
        
        // 如果没有子命令匹配，补全路径
        if len(completions) == 0 {
            completions = completePath(ctx.WordToComplete)
        }
    }
    
    return types.CompletionResult{
        Completions: completions,
        Directive:   0,
    }
}

// completeFlagName 补全标志名（长标志和短标志）
func completeFlagName(ctx *Context) []types.Completion {
    var completions []types.Completion
    word := ctx.WordToComplete
    
    for _, flag := range ctx.CurrentCmd.Flags() {
        // 补全长标志
        if longName := flag.LongName(); longName != "" {
            if strings.HasPrefix(longName, word) {
                completions = append(completions, types.Completion{
                    Value:       longName,
                    Description: flag.Desc(),
                })
            }
        }
        
        // 补全短标志
        if shortName := flag.ShortName(); shortName != "" {
            shortFlag := "-" + shortName
            if strings.HasPrefix(shortFlag, word) {
                completions = append(completions, types.Completion{
                    Value:       shortFlag,
                    Description: flag.Desc(),
                })
            }
        }
    }
    return completions
}

// completeSubCommand 补全子命令
func completeSubCommand(ctx *Context) []types.Completion {
    var completions []types.Completion
    for _, subCmd := range ctx.CurrentCmd.SubCmds() {
        // 跳过隐藏子命令（如 __complete）
        if subCmd.IsHidden() {
            continue
        }
        name := subCmd.Name()
        if strings.HasPrefix(name, ctx.WordToComplete) {
            completions = append(completions, types.Completion{
                Value:       name,
                Description: subCmd.Desc(),
            })
        }
    }
    return completions
}

// completeFlagValue 补全标志值
func completeFlagValue(ctx *Context) []types.Completion {
    var completions []types.Completion
    
    flagName := strings.TrimLeft(ctx.PreviousWord, "-")
    if flag, ok := ctx.CurrentCmd.GetFlag(flagName); ok {
        // 如果是枚举类型，返回枚举值
        if enumValues := flag.EnumValues(); len(enumValues) > 0 {
            for _, opt := range enumValues {
                if strings.HasPrefix(opt, ctx.WordToComplete) {
                    completions = append(completions, types.Completion{Value: opt})
                }
            }
            return completions
        }
    }
    
    // 默认补全路径
    return completePath(ctx.WordToComplete)
}

// completePath 补全文件路径
func completePath(word string) []types.Completion {
    var completions []types.Completion
    
    // 解析路径前缀
    dir := "."
    prefix := ""
    if idx := strings.LastIndex(word, "/"); idx >= 0 {
        dir = word[:idx]
        if dir == "" {
            dir = "/"
        }
        prefix = word[idx+1:]
    } else if idx := strings.LastIndex(word, "\\"); idx >= 0 {
        dir = word[:idx]
        if dir == "" {
            dir = "\\"
        }
        prefix = word[idx+1:]
    } else {
        prefix = word
    }
    
    // 读取目录内容
    entries, err := os.ReadDir(dir)
    if err != nil {
        return completions
    }
    
    for _, entry := range entries {
        name := entry.Name()
        if strings.HasPrefix(name, prefix) {
            value := name
            if entry.IsDir() {
                value += "/"
            }
            completions = append(completions, types.Completion{Value: value})
        }
    }
    
    return completions
}

// PrintResult 打印补全结果
func PrintResult(result types.CompletionResult) {
    for _, comp := range result.Completions {
        if comp.Description != "" {
            fmt.Printf("%s\t%s\n", comp.Value, comp.Description)
        } else {
            fmt.Println(comp.Value)
        }
    }
    fmt.Printf(":%d\n", result.Directive)
}
```

## 使用方式

### 用户代码

```go
package main

import (
    "os"
    "gitee.com/MM-Q/qflag"
    "gitee.com/MM-Q/qflag/internal/types"
)

func main() {
    // 创建命令
    root := qflag.NewCmd("myapp", "我的应用", qflag.ExitOnError)
    
    // 启用补全（必须）
    root.SetCompletion(true)
    
    // 启用动态补全（可选，默认静态）
    root.SetDynamicCompletion(true)
    
    // 创建子命令
    deploy := qflag.NewCmd("deploy", "部署应用", qflag.ExitOnError)
    deploy.String("env", "e", "部署环境", "dev")
    
    // 注册自定义补全
    deploy.RegisterCompleter("env", func(ctx types.CompletionContext) types.CompletionResult {
        return types.CompletionResult{
            Completions: []types.Completion{
                {Value: "dev", Description: "开发环境"},
                {Value: "prod", Description: "生产环境"},
            },
        }
    })
    
    root.AddSubCmds(deploy)
    
    // 执行
    if err := root.ParseAndRoute(os.Args[1:]); err != nil {
        os.Exit(1)
    }
}
```

### 生成补全脚本

```bash
# 启用动态补全后，生成的是动态补全脚本
myapp --completion=bash

# 只启用补全（不启用动态），生成的是静态补全脚本
```

## 执行流程

```
1. 用户在 Shell 中输入: myapp deploy --env <Tab>
   
2. Shell 执行补全脚本中的函数
   
3. 动态脚本调用: myapp __complete "" "--env" "myapp" "deploy" "--env" ""
   
   参数说明:
   - "": 当前词（空表示刚输入空格）
   - "--env": 前一个词
   - "myapp" "deploy" "--env" "": 完整命令行

4. qflag 解析 __complete 子命令:
   - 作为普通子命令解析
   - 获取完整的命令树
   - 执行补全逻辑
   
5. 输出补全结果
   dev	开发环境
   prod	生产环境
   :0
   
6. Shell 展示给用户
```

## 文件变更清单

### 新增文件

1. `internal/completion/context.go` - 补全上下文
2. `internal/completion/execute.go` - 补全执行逻辑
3. `internal/completion/templates/bash_dynamic.tmpl` - Bash 动态模板
4. `internal/completion/templates/pwsh_dynamic.tmpl` - PowerShell 动态模板

### 修改文件

1. `internal/completion/completion.go` - 修改 Generate 函数，支持动态脚本生成
2. `internal/cmd/cmd.go` - 添加 SetDynamicCompletion、RegisterCompleter、createCompleteCmd
3. `internal/cmd/cmdopts.go` - 添加 DynamicCompletion 选项
4. `internal/types/builtin.go` - 添加补全相关类型
5. `internal/types/config.go` - 添加 DynamicCompletion 字段
6. `internal/types/command.go` - 添加 GetCompleter、IsHidden 接口方法
7. `internal/help/help.go` - 添加对 `__complete` 子命令的跳过逻辑

### 需要添加的接口方法

**位置**: `internal/types/command.go`

```go
// Command 接口添加以下方法：

// GetCompleter 获取指定标志的自定义补全函数
// 参数:
//   - flagName: 标志名（长名或短名）
// 返回值:
//   - CompletionFunc: 补全函数
//   - bool: 是否存在
GetCompleter(flagName string) (CompletionFunc, bool)

// IsHidden 判断命令是否隐藏
// 返回值:
//   - bool: 是否隐藏（隐藏命令不在帮助中显示）
IsHidden() bool
```

### 帮助信息生成修改

**位置**: `internal/help/help.go`

在生成子命令帮助信息时，跳过隐藏的子命令：

```go
// formatSubCommands 格式化子命令列表
func formatSubCommands(cmd types.Command, lang string) string {
    // ... 现有代码 ...
    
    for _, subCmd := range subCmds {
        // 【新增】跳过隐藏的子命令
        if subCmd.IsHidden() {
            continue
        }
        
        // ... 继续生成帮助信息 ...
    }
    
    // ... 现有代码 ...
}
```

**说明**: 通过 `IsHidden()` 方法统一处理隐藏子命令，不需要根据名字判断，更加通用。

## 总结

- **用户接口**: 只需要 `--completion=bash`，简化使用
- **内部判断**: 根据 `Completion` 和 `DynamicCompletion` 两个字段决定生成什么脚本
- **动态补全**: 通过 `__complete` 子命令执行，享受完整解析流程
